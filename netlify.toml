# Netlify configuration for Accelerator X website
# Configures build, functions, and security headers

[build]
  publish = "."
  # Build artefacts (index.html, insights/, sitemap.xml) are pre-built locally and committed.
  # Netlify serves the repo root as-is. Run `npm run build` locally before every push.
  command = "echo 'Static site ready — pre-built artefacts committed to repo'"

# Functions directory for serverless functions
[functions]
  directory = "netlify/functions"

# Security headers — defined ONLY here (never in the cache blocks below: overlapping
# Netlify header rules emit duplicate headers, which is undefined behaviour).
#
# CSP notes (2026-07-04 hardening, #78):
# - script-src hash authorises the one inline handler used site-wide:
#   onload="this.onload=null;this.rel='stylesheet'" (Google Fonts async-load trick,
#   byte-identical across all 34 pages — recompute the hash if that string ever changes:
#   printf %s "<handler>" | openssl dgst -sha256 -binary | base64).
# - eu-assets.i.posthog.com serves the PostHog loader + lazy chunks; eu.i.posthog.com
#   receives event/flag POSTs (connect-src).
# - style-src fonts.googleapis.com also covers the Figtree @import inside
#   tokens.css / tailwind.generated.css; 'unsafe-inline' covers inline style="" attrs.
# - img-src data: is required — CSS mask-image data-URIs (FitCheck, FormInputs) are
#   governed by img-src.
# - HSTS deliberately has no includeSubDomains (quiz/mail subdomains) and no preload
#   (irreversible); both can be added later as one-word edits.
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Strict-Transport-Security = "max-age=31536000"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-hashes' 'sha256-1jAmyYXcRq6zFldLe/GCgIDJBiOONdXjTLgEFMDnDSM=' https://eu-assets.i.posthog.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data:; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://eu.i.posthog.com https://eu-assets.i.posthog.com; form-action 'self'; base-uri 'self'; frame-ancestors 'none'; object-src 'none'"

# Caching — deliberately NO custom Cache-Control for css/js/images/fonts.
#
# This repo's filenames are not fingerprinted, so we previously hand-set
# `max-age=31536000, immutable` on all static assets — a real bug: a deploy could
# leave returning visitors on stale CSS/JS for up to a year (found 2026-07-04, #78).
# The first fix attempt replaced it with three custom tiers (css/js must-revalidate,
# images swr, fonts immutable) using `/**/*.@(ext1|ext2)` extglob-style globs — but
# Netlify's header path matcher does not support that syntax, so none of the three
# rules actually matched anything (confirmed live: HTML, JS, fonts, and images all
# returned the identical, unrelated fallback value). The custom rules were dead code
# that happened to look like it was working.
#
# The correct fix is simpler: remove the custom override entirely. Netlify's own
# documented default for static assets already does the safe thing with no
# configuration — CDN-level caching for 1 year, auto-invalidated on every deploy,
# with the browser tier always set to `max-age=0, must-revalidate` (see the
# netlify-caching skill / Netlify docs). That is exactly the staleness-safe behaviour
# we were trying to hand-roll, and it's deploy-aware in a way a static max-age can't
# be. Only add a custom Cache-Control here again for genuinely fingerprinted assets
# (e.g. `/assets/*.<hash>.css`) — verify any header `for` glob against a live deploy
# (`curl -sI <url>`) before trusting it; extglob alternation is not supported.

# Keep HTML short-lived to pick up updates
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=300"

# Quiz path 301s (#81): the quiz lives on its own subdomain (site-config.js QUIZ_URL);
# nothing on the site links to /quiz/* any more, but the path was published
# historically (the pre-#78 proxy config), so old or bookmarked inbound links should
# resolve to the live quiz rather than 404. The subdomain serves the assessment at
# its root — deep paths like /quiz/aireadiness no longer exist there (verified live
# 2026-08-05), so every /quiz path collapses to the root. This supersedes the
# unmerged claude/crazy-shirley-540e0d branch, whose 301s targeted that now-dead
# deep path. (The #78 removal note this replaces covered the same paths as broken
# status-200 proxies to a placeholder host.)
[[redirects]]
  from = "/quiz"
  to = "https://quiz.accelerator-x.ai/"
  status = 301
  force = true

[[redirects]]
  from = "/quiz/*"
  to = "https://quiz.accelerator-x.ai/"
  status = 301
  force = true
